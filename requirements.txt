# nuImages nuMini + YOLO starter
# IMPORTANT: Use Python 3.9, 3.10, 3.11 or 3.12 (not 3.13).
# nuscenes-devkit needs numpy<2, which has no pre-built wheel for 3.13;
# pip would try to build numpy from source and fail on Windows without a C compiler.
# Create a conda env:  conda create -n nuimages python=3.12
# Then:  conda activate nuimages   and   pip install -r requirements.txt
nuscenes-devkit
ultralytics
matplotlib
opencv-python
PyYAML
