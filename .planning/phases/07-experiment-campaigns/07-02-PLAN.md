---
phase: 07-experiment-campaigns
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - BackEnd/scripts/run_extension_ablation.py
  - BackEnd/tests/test_ablation_configs.py
autonomous: true
requirements: [CAMP-02, CAMP-03, CAMP-04]

must_haves:
  truths:
    - "Extension ablation script defines FedProx configs with mu in {0.0, 0.01, 0.1}"
    - "Extension ablation script defines cooperative reward configs with alpha in {1.0, 0.5, 0.1}"
    - "Extension ablation script defines time-of-day configs (off vs on+encoding)"
    - "Each ablation trains fresh weights then evaluates with 10 MC seeds"
    - "Results are saved per-ablation as named campaign directories"
  artifacts:
    - path: "BackEnd/scripts/run_extension_ablation.py"
      provides: "CLI script for running all three extension ablation studies"
      contains: "run_fedprox_ablation"
    - path: "BackEnd/tests/test_ablation_configs.py"
      provides: "Unit tests validating ablation config correctness"
      contains: "test_fedprox_configs"
  key_links:
    - from: "BackEnd/scripts/run_extension_ablation.py"
      to: "BackEnd/scripts/run_campaign.py"
      via: "import train_and_evaluate, save_campaign_results"
      pattern: "from scripts\\.run_campaign import"
    - from: "BackEnd/scripts/run_extension_ablation.py"
      to: "BackEnd/api/evaluation/campaign_config.py"
      via: "import ExtensionConfig"
      pattern: "from api\\.evaluation\\.campaign_config import"
---

<objective>
Create the extension ablation script that runs FedProx, cooperative reward, and time-of-day experiments as parameterized campaigns.

Purpose: These are the three core extension experiments from Phase 4 that need empirical validation. Each ablation trains with specific parameter values, then evaluates with 10 MC seeds to produce statistically comparable results.

Output: `run_extension_ablation.py` (CLI script with all three ablation studies), `test_ablation_configs.py` (config validation tests)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-experiment-campaigns/07-RESEARCH.md
@.planning/phases/07-experiment-campaigns/07-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From BackEnd/api/evaluation/campaign_config.py:
```python
@dataclass
class ExtensionConfig:
    name: str
    trainer_type: str
    topology: str
    n_episodes: int = 50
    n_eval_runs: int = 10
    base_seed: int = 42
    ranked: bool = True
    horizon: int = 450
    fedprox_mu: float = 0.0
    alpha: float = 1.0
    time_of_day: bool = False
    use_time_encoding: bool = False
    weights_path: Optional[str] = None

@dataclass
class CampaignResult:
    config: ExtensionConfig
    training_rewards: Optional[List[dict]] = None
    evaluation: Optional[MCAggregatedResult] = None
    weights_path: Optional[str] = None
    error: Optional[str] = None
    duration_seconds: float = 0.0
```

From BackEnd/scripts/run_campaign.py:
```python
def train_and_evaluate(config: ExtensionConfig, on_progress=None) -> CampaignResult
def save_campaign_results(results: List[CampaignResult], campaign_name: str) -> str
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create extension ablation script with all three experiment definitions</name>
  <files>BackEnd/scripts/run_extension_ablation.py</files>
  <read_first>
    - BackEnd/scripts/run_campaign.py (train_and_evaluate, save_campaign_results signatures)
    - BackEnd/api/evaluation/campaign_config.py (ExtensionConfig dataclass)
    - BackEnd/api/training_runner.py (create_trainer params to verify param names match)
  </read_first>
  <action>
Create `BackEnd/scripts/run_extension_ablation.py` — CLI script for running extension ablation studies.

Add BackEnd to sys.path at top:
```python
import sys, os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))
```

Implement three ablation functions, each returning `List[CampaignResult]`:

1. `build_fedprox_configs(topology="grid-3x3", n_episodes=50, n_eval_runs=10) -> List[ExtensionConfig]`:
   Returns 3 configs:
   - `ExtensionConfig(name="fedavg_baseline", trainer_type="FedRL", topology=topology, n_episodes=n_episodes, n_eval_runs=n_eval_runs, fedprox_mu=0.0)`
   - `ExtensionConfig(name="fedprox_mu0.01", trainer_type="FedRL", topology=topology, n_episodes=n_episodes, n_eval_runs=n_eval_runs, fedprox_mu=0.01)`
   - `ExtensionConfig(name="fedprox_mu0.1", trainer_type="FedRL", topology=topology, n_episodes=n_episodes, n_eval_runs=n_eval_runs, fedprox_mu=0.1)`

2. `build_cooperative_configs(topology="grid-3x3", n_episodes=50, n_eval_runs=10) -> List[ExtensionConfig]`:
   Returns 3 configs (per research note: do NOT use alpha=0.0 — degenerate rewards):
   - `ExtensionConfig(name="selfish_baseline", trainer_type="FedRL", topology=topology, n_episodes=n_episodes, n_eval_runs=n_eval_runs, alpha=1.0)`
   - `ExtensionConfig(name="cooperative_alpha0.5", trainer_type="FedRL", topology=topology, n_episodes=n_episodes, n_eval_runs=n_eval_runs, alpha=0.5)`
   - `ExtensionConfig(name="cooperative_alpha0.1", trainer_type="FedRL", topology=topology, n_episodes=n_episodes, n_eval_runs=n_eval_runs, alpha=0.1)`

3. `build_time_of_day_configs(topology="grid-3x3", n_episodes=50, n_eval_runs=10) -> List[ExtensionConfig]`:
   Returns 2 configs:
   - `ExtensionConfig(name="fixed_demand", trainer_type="FedRL", topology=topology, n_episodes=n_episodes, n_eval_runs=n_eval_runs, time_of_day=False, use_time_encoding=False)`
   - `ExtensionConfig(name="tod_with_encoding", trainer_type="FedRL", topology=topology, n_episodes=n_episodes, n_eval_runs=n_eval_runs, time_of_day=True, use_time_encoding=True)`

4. `run_ablation(configs: List[ExtensionConfig], ablation_name: str, dry_run_seeds: Optional[int] = None) -> List[CampaignResult]`:
   - If dry_run_seeds is set, override each config's n_eval_runs to that value
   - Loop through configs, call `train_and_evaluate(config)` for each
   - Log: `logger.info("Ablation %s: running config '%s' (%d/%d)", ablation_name, config.name, i+1, len(configs))`
   - Call `save_campaign_results(results, ablation_name)` after all configs complete
   - Return the list of CampaignResult

5. `main()` with argparse:
   - `--ablation` choices: `["fedprox", "cooperative", "time-of-day", "all"]` required
   - `--topology` default="grid-3x3"
   - `--n-episodes` type=int default=50
   - `--n-eval-runs` type=int default=10
   - `--dry-run` type=int default=None — override n_eval_runs for smoke testing
   - Dispatches to the appropriate build_*_configs + run_ablation
   - For "all": runs all three ablations sequentially, each saved under its own campaign name ("fedprox-ablation", "cooperative-ablation", "tod-ablation")
   - Prints summary table at end: config name, n_completed, n_failed, avg_waiting_time mean (if available), duration_seconds

Add `if __name__ == "__main__": main()` at bottom.

IMPORTANT per research Pitfall 6: alpha values are {1.0, 0.5, 0.1} NOT {1.0, 0.5, 0.0}. Add a comment: "# alpha=0.0 excluded — creates degenerate zero reward signal (see research pitfall 6)"
  </action>
  <verify>
    <automated>cd BackEnd && python -c "from scripts.run_extension_ablation import build_fedprox_configs, build_cooperative_configs, build_time_of_day_configs; fp = build_fedprox_configs(); assert len(fp) == 3; co = build_cooperative_configs(); assert len(co) == 3; td = build_time_of_day_configs(); assert len(td) == 2; print('all config builders OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `BackEnd/scripts/run_extension_ablation.py` exists
    - grep "def build_fedprox_configs" exists
    - grep "def build_cooperative_configs" exists
    - grep "def build_time_of_day_configs" exists
    - grep "def run_ablation" exists
    - grep "fedprox_mu=0.01" appears in fedprox configs
    - grep "fedprox_mu=0.1" appears in fedprox configs
    - grep "alpha=0.5" appears in cooperative configs
    - grep "alpha=0.1" appears in cooperative configs
    - grep "alpha=0.0" does NOT appear as a config value (only in comments)
    - grep "time_of_day=True" appears in tod configs
    - grep "use_time_encoding=True" appears in tod configs
    - The import and config builder smoke test passes
  </acceptance_criteria>
  <done>All three ablation config builders return correctly parameterized ExtensionConfigs. Alpha=0.0 is excluded. Script can be invoked via `python scripts/run_extension_ablation.py --ablation fedprox --dry-run 1`.</done>
</task>

<task type="auto">
  <name>Task 2: Create ablation config validation tests</name>
  <files>BackEnd/tests/test_ablation_configs.py</files>
  <read_first>
    - BackEnd/scripts/run_extension_ablation.py (just created in Task 1)
    - BackEnd/api/evaluation/campaign_config.py (ExtensionConfig)
  </read_first>
  <action>
Create `BackEnd/tests/test_ablation_configs.py` with tests:

1. `test_fedprox_configs_count_and_mu_values()`:
   - Call `build_fedprox_configs()`
   - Assert length is 3
   - Assert mu values are exactly `[0.0, 0.01, 0.1]` (extract with list comprehension)
   - Assert all trainer_type are "FedRL"

2. `test_cooperative_configs_no_zero_alpha()`:
   - Call `build_cooperative_configs()`
   - Assert length is 3
   - Assert alpha values are exactly `[1.0, 0.5, 0.1]`
   - Assert 0.0 not in alpha values (safety check for pitfall 6)

3. `test_time_of_day_configs_encoding_pairing()`:
   - Call `build_time_of_day_configs()`
   - Assert length is 2
   - Assert config[0].time_of_day is False and config[0].use_time_encoding is False
   - Assert config[1].time_of_day is True and config[1].use_time_encoding is True

4. `test_configs_respect_topology_override()`:
   - Call `build_fedprox_configs(topology="grid-5x5")`
   - Assert all configs have topology == "grid-5x5"

5. `test_configs_respect_episodes_override()`:
   - Call `build_fedprox_configs(n_episodes=30)`
   - Assert all configs have n_episodes == 30

All tests import from `scripts.run_extension_ablation` using the `sys.path.insert(0, ...)` pattern at the top of the test file pointing to `BackEnd/`.
  </action>
  <verify>
    <automated>cd BackEnd && python -m pytest tests/test_ablation_configs.py -x -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File `BackEnd/tests/test_ablation_configs.py` exists
    - grep "test_fedprox_configs" exists
    - grep "test_cooperative_configs_no_zero_alpha" exists
    - grep "test_time_of_day_configs_encoding_pairing" exists
    - grep "0.0 not in" or equivalent assertion exists for alpha safety check
    - All 5 tests pass
  </acceptance_criteria>
  <done>All ablation config builder tests pass. Alpha=0.0 exclusion is verified by test. Topology and episode overrides are verified.</done>
</task>

</tasks>

<verification>
- `cd BackEnd && python -m pytest tests/test_ablation_configs.py -x -v` — all 5 tests pass
- `cd BackEnd && python -c "from scripts.run_extension_ablation import build_fedprox_configs, build_cooperative_configs, build_time_of_day_configs"` — imports succeed
- `grep -c "alpha=0.0" BackEnd/scripts/run_extension_ablation.py` — appears only in comments, not as a config value
</verification>

<success_criteria>
- Three ablation studies (FedProx, cooperative, time-of-day) are fully parameterized
- Alpha=0.0 is excluded per research pitfall guidance
- Each ablation can be run independently or all together via --ablation flag
- Dry-run flag enables 1-2 seed smoke tests before committing to full runs
- Config validation tests confirm correct parameter values
</success_criteria>

<output>
After completion, create `.planning/phases/07-experiment-campaigns/07-02-SUMMARY.md`
</output>
