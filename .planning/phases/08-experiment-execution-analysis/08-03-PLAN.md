---
phase: 08-experiment-execution-analysis
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - BackEnd/results/campaigns/fedprox-ablation/results.json
  - BackEnd/results/campaigns/cooperative-ablation/results.json
  - BackEnd/results/campaigns/tod-ablation/results.json
  - BackEnd/results/figures/chart_convergence_fedprox.png
  - BackEnd/results/figures/chart_convergence_cooperative.png
  - BackEnd/results/figures/chart_convergence_tod.png
  - BackEnd/results/figures/chart_combined_comparison.png
  - BackEnd/results/figures/chart_topology_grid-3x3.png
  - BackEnd/results/figures/chart_topology_grid-5x5.png
  - BackEnd/results/figures/table_fedprox_ablation.tex
  - BackEnd/results/figures/table_cooperative_ablation.tex
  - BackEnd/results/figures/table_combined_extensions.tex
  - BackEnd/results/figures/EXPERIMENT_REPORT.md
autonomous: false
requirements: [EXP-02, EXP-03, EXP-04, EXP-05]

must_haves:
  truths:
    - "FedProx ablation runs with mu in {0.0, 0.01, 0.1} on both topologies"
    - "Cooperative ablation runs with alpha in {1.0, 0.5, 0.1} on both topologies"
    - "Time-of-day ablation runs fixed vs ToD+encoding on both topologies"
    - "Training convergence curves generated for each ablation"
    - "Ablation LaTeX tables include Wilcoxon p-values"
    - "Combined extensions table compares baseline vs best from each ablation"
    - "Per-topology charts show extension effects on grid-3x3 and grid-5x5 separately"
    - "EXPERIMENT_REPORT.md ties together all tables, charts, and findings"
  artifacts:
    - path: "BackEnd/results/campaigns/fedprox-ablation/results.json"
      provides: "FedProx ablation results with 3 mu values x 2 topologies"
      contains: "results"
    - path: "BackEnd/results/campaigns/cooperative-ablation/results.json"
      provides: "Cooperative ablation results with 3 alpha values x 2 topologies"
      contains: "results"
    - path: "BackEnd/results/campaigns/tod-ablation/results.json"
      provides: "Time-of-day ablation results with 2 configs x 2 topologies"
      contains: "results"
    - path: "BackEnd/results/figures/EXPERIMENT_REPORT.md"
      provides: "Complete experiment report with Key Findings, all tables/charts referenced"
      contains: "Key Findings"
    - path: "BackEnd/results/figures/table_combined_extensions.tex"
      provides: "Cross-ablation comparison table (D-12)"
      contains: "\\toprule"
    - path: "BackEnd/results/figures/chart_combined_comparison.png"
      provides: "Combined comparison bar chart (D-15)"
  key_links:
    - from: "BackEnd/scripts/run_extension_ablation.py"
      to: "BackEnd/results/campaigns/*/results.json"
      via: "save_campaign_results()"
      pattern: "results\\.json"
    - from: "BackEnd/scripts/generate_report.py"
      to: "BackEnd/results/figures/EXPERIMENT_REPORT.md"
      via: "generate_experiment_report()"
      pattern: "EXPERIMENT_REPORT"
---

<objective>
Run all three extension ablation campaigns (FedProx, cooperative reward, time-of-day) against
live SUMO on both topologies, then generate all publishable analysis artifacts and the final
EXPERIMENT_REPORT.md.

Purpose: EXP-02/03/04 require running each ablation with fresh training. EXP-05 requires
generating all statistical analysis and figures. D-04 specifies running ablations sequentially
after baseline (which was approved in Plan 02's checkpoint). D-12/D-14/D-15/D-16/D-17 specify
the new analysis artifacts that synthesize across all campaigns.

Output: Three ablation results.json files, convergence curve PNGs, ablation LaTeX tables
with p-values, combined extensions table, per-topology charts, combined comparison chart,
and the final EXPERIMENT_REPORT.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-experiment-execution-analysis/08-CONTEXT.md
@.planning/phases/08-experiment-execution-analysis/08-01-SUMMARY.md
@.planning/phases/08-experiment-execution-analysis/08-02-SUMMARY.md

<interfaces>
From BackEnd/scripts/run_extension_ablation.py CLI (updated in Plan 01):
```
--ablation {fedprox,cooperative,time-of-day,all}
--topologies grid-3x3 grid-5x5   # NEW from Plan 01
--n-episodes 50                    # per D-02
--n-eval-runs 10                   # per D-03
--dry-run N                        # Override seeds for smoke test
```

From BackEnd/scripts/generate_report.py CLI (created in Plan 01):
```
--results-dir results/campaigns    # Base directory containing all campaign subdirs
--output-dir results/figures       # Output for figures + EXPERIMENT_REPORT.md
```

Campaign directory names (per Phase 7 decisions):
- baseline -> results/campaigns/baseline/
- fedprox-ablation -> results/campaigns/fedprox-ablation/
- cooperative-ablation -> results/campaigns/cooperative-ablation/
- tod-ablation -> results/campaigns/tod-ablation/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all three ablation campaigns sequentially on both topologies</name>
  <files>
    BackEnd/results/campaigns/fedprox-ablation/results.json,
    BackEnd/results/campaigns/cooperative-ablation/results.json,
    BackEnd/results/campaigns/tod-ablation/results.json
  </files>
  <read_first>
    BackEnd/scripts/run_extension_ablation.py,
    BackEnd/api/evaluation/campaign_config.py,
    .planning/phases/08-experiment-execution-analysis/08-CONTEXT.md
  </read_first>
  <action>
    **Run all ablations sequentially per D-04, on both topologies per D-01:**

    The --topologies flag (added in Plan 01) makes this a single invocation per ablation.
    Run each ablation separately (not --ablation all) to isolate failures:

    ```bash
    # FedProx ablation: mu in {0.0, 0.01, 0.1} (per D-10, EXP-02)
    cd BackEnd && python scripts/run_extension_ablation.py \
        --ablation fedprox \
        --topologies grid-3x3 grid-5x5 \
        --n-episodes 50 \
        --n-eval-runs 10
    ```

    Verify fedprox results saved, then proceed:

    ```bash
    # Cooperative ablation: alpha in {1.0, 0.5, 0.1} (per D-11, EXP-03)
    cd BackEnd && python scripts/run_extension_ablation.py \
        --ablation cooperative \
        --topologies grid-3x3 grid-5x5 \
        --n-episodes 50 \
        --n-eval-runs 10
    ```

    Verify cooperative results saved, then proceed:

    ```bash
    # Time-of-day ablation: fixed vs ToD+encoding (per EXP-04)
    cd BackEnd && python scripts/run_extension_ablation.py \
        --ablation time-of-day \
        --topologies grid-3x3 grid-5x5 \
        --n-episodes 50 \
        --n-eval-runs 10
    ```

    Per D-07: on failure, errors are logged in CampaignResult, failed configs are skipped,
    remaining configs continue. Per D-08: no automatic retries.

    After all three ablations complete, verify all results.json files exist:
    ```bash
    ls -la BackEnd/results/campaigns/fedprox-ablation/results.json
    ls -la BackEnd/results/campaigns/cooperative-ablation/results.json
    ls -la BackEnd/results/campaigns/tod-ablation/results.json
    ```
  </action>
  <verify>
    <automated>cd C:/Users/user/Documents/GitHub/RoadsidePrediction/BackEnd && python -c "
import json, os
for name in ['fedprox-ablation', 'cooperative-ablation', 'tod-ablation']:
    path = f'results/campaigns/{name}/results.json'
    assert os.path.exists(path), f'Missing: {path}'
    data = json.load(open(path))
    n = len(data['results'])
    print(f'{name}: {n} configs')
    assert n >= 2, f'Expected >=2 configs in {name}, got {n}'
print('PASS: all ablation results exist')
"</automated>
  </verify>
  <acceptance_criteria>
    - BackEnd/results/campaigns/fedprox-ablation/results.json exists with >= 3 config entries (3 mu values, possibly x2 topologies = 6)
    - BackEnd/results/campaigns/cooperative-ablation/results.json exists with >= 3 config entries
    - BackEnd/results/campaigns/tod-ablation/results.json exists with >= 2 config entries
    - Each results.json contains "results" key with list of result dicts
    - Each result dict has "config", "evaluation", and "duration_seconds" fields
  </acceptance_criteria>
  <done>
    All three ablation campaigns completed on both topologies. Results persisted as JSON
    in their respective campaign directories.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate all analysis artifacts and EXPERIMENT_REPORT.md</name>
  <files>
    BackEnd/results/figures/chart_convergence_fedprox.png,
    BackEnd/results/figures/chart_convergence_cooperative.png,
    BackEnd/results/figures/chart_convergence_tod.png,
    BackEnd/results/figures/chart_combined_comparison.png,
    BackEnd/results/figures/chart_topology_grid-3x3.png,
    BackEnd/results/figures/chart_topology_grid-5x5.png,
    BackEnd/results/figures/table_fedprox_ablation.tex,
    BackEnd/results/figures/table_cooperative_ablation.tex,
    BackEnd/results/figures/table_combined_extensions.tex,
    BackEnd/results/figures/EXPERIMENT_REPORT.md
  </files>
  <read_first>
    BackEnd/scripts/generate_report.py,
    BackEnd/scripts/generate_tables.py,
    .planning/phases/08-experiment-execution-analysis/08-CONTEXT.md
  </read_first>
  <action>
    **Run the report generator (per D-17, D-18, EXP-05):**

    ```bash
    cd BackEnd && python scripts/generate_report.py \
        --results-dir results/campaigns \
        --output-dir results/figures
    ```

    This single invocation should:
    1. Load all four campaign directories (baseline, fedprox-ablation, cooperative-ablation, tod-ablation)
    2. Generate per-campaign outputs (tables, bar charts) via generate_all_outputs()
    3. Generate convergence curves for each ablation (D-14): chart_convergence_fedprox.png, etc.
    4. Generate ablation tables with p-values (D-10, D-11): table_fedprox_ablation.tex, table_cooperative_ablation.tex
    5. Generate combined extensions table (D-12): table_combined_extensions.tex
    6. Generate combined comparison chart (D-15): chart_combined_comparison.png
    7. Generate per-topology charts (D-16): chart_topology_grid-3x3.png, chart_topology_grid-5x5.png
    8. Write EXPERIMENT_REPORT.md (D-17) to results/figures/EXPERIMENT_REPORT.md (D-18)

    After generation, verify key files exist:
    ```bash
    ls -la BackEnd/results/figures/EXPERIMENT_REPORT.md
    ls -la BackEnd/results/figures/table_combined_extensions.tex
    ls -la BackEnd/results/figures/chart_combined_comparison.png
    ls -la BackEnd/results/figures/chart_convergence_fedprox.png
    ```

    Read and display the Key Findings section of the report so the user can review.
  </action>
  <verify>
    <automated>cd C:/Users/user/Documents/GitHub/RoadsidePrediction/BackEnd && python -c "
import os
required = [
    'results/figures/EXPERIMENT_REPORT.md',
    'results/figures/table_combined_extensions.tex',
    'results/figures/chart_combined_comparison.png',
]
for f in required:
    assert os.path.exists(f), f'Missing: {f}'
    assert os.path.getsize(f) > 0, f'Empty: {f}'
report = open('results/figures/EXPERIMENT_REPORT.md').read()
assert 'Key Findings' in report, 'Report missing Key Findings section'
assert 'SEAL Experiment Report' in report, 'Report missing title'
print(f'EXPERIMENT_REPORT.md: {len(report)} chars')
print('PASS: all analysis artifacts generated')
"</automated>
  </verify>
  <acceptance_criteria>
    - BackEnd/results/figures/EXPERIMENT_REPORT.md exists, is > 0 bytes, contains "Key Findings" and "SEAL Experiment Report"
    - BackEnd/results/figures/table_combined_extensions.tex exists and contains `\toprule`
    - BackEnd/results/figures/chart_combined_comparison.png exists and is > 0 bytes
    - BackEnd/results/figures/chart_convergence_fedprox.png exists and is > 0 bytes (training_rewards present for ablation runs)
    - At least one of table_fedprox_ablation.tex or table_cooperative_ablation.tex contains `p=`
  </acceptance_criteria>
  <done>
    All publishable analysis artifacts generated: convergence curves, ablation tables with
    p-values, combined extensions table, combined comparison chart, per-topology charts,
    and EXPERIMENT_REPORT.md with Key Findings summary.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User reviews experiment report and analysis artifacts</name>
  <files>
    BackEnd/results/figures/EXPERIMENT_REPORT.md,
    BackEnd/results/figures/table_combined_extensions.tex,
    BackEnd/results/figures/chart_combined_comparison.png
  </files>
  <action>
    Display the experiment report and key artifacts for user review. Read and present:
    1. The full EXPERIMENT_REPORT.md content (especially Key Findings section)
    2. The combined extensions table from table_combined_extensions.tex
    3. The combined comparison chart from chart_combined_comparison.png
    4. A summary of which ablation conditions showed statistically significant improvements
  </action>
  <verify>
    <automated>cd C:/Users/user/Documents/GitHub/RoadsidePrediction/BackEnd && python -c "import os; assert os.path.exists('results/figures/EXPERIMENT_REPORT.md'); print('PASS: report ready for review')"</automated>
  </verify>
  <acceptance_criteria>
    - User has read the EXPERIMENT_REPORT.md Key Findings section
    - User has reviewed convergence curves and significance test results
    - User has typed "approved" or provided specific feedback for revisions
  </acceptance_criteria>
  <done>
    User has reviewed and approved the complete experiment report and all analysis artifacts.
    Phase 8 is complete.
  </done>
</task>

</tasks>

<verification>
1. All 3 ablation results.json files exist with correct number of configs
2. EXPERIMENT_REPORT.md exists with Key Findings and chart/table references
3. All chart PNGs are non-empty
4. All LaTeX tables contain booktabs formatting
5. User approves the final report
</verification>

<success_criteria>
All four experiment campaigns completed (baseline + 3 ablations), all publishable artifacts
generated (LaTeX tables, bar charts, convergence curves, combined comparisons, per-topology
breakdowns), and EXPERIMENT_REPORT.md ready for paper draft integration. User has reviewed
and approved the results.
</success_criteria>

<output>
After completion, create `.planning/phases/08-experiment-execution-analysis/08-03-SUMMARY.md`
</output>
