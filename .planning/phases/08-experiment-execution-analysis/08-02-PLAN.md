---
phase: 08-experiment-execution-analysis
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - BackEnd/results/campaigns/baseline/results.json
  - BackEnd/results/campaigns/baseline/config.json
  - BackEnd/results/figures/table_avg_waiting_time.tex
  - BackEnd/results/figures/chart_avg_waiting_time.png
autonomous: false
requirements: [EXP-01]

must_haves:
  truths:
    - "Baseline dry-run (1 seed) completes without error on grid-3x3"
    - "Full baseline campaign (10 seeds) completes for FedRL, MARL, SARL, fixed-time, max-pressure on grid-3x3 and grid-5x5"
    - "Baseline results.json contains entries for all 5 trainer types"
    - "User reviews baseline avg wait times before proceeding to ablations"
  artifacts:
    - path: "BackEnd/results/campaigns/baseline/results.json"
      provides: "Baseline campaign results for all trainer/topology pairs"
      contains: "results"
    - path: "BackEnd/results/campaigns/baseline/config.json"
      provides: "Campaign configuration metadata"
      contains: "campaign"
    - path: "BackEnd/results/figures/table_avg_waiting_time.tex"
      provides: "LaTeX baseline comparison table (D-09)"
      contains: "\\toprule"
    - path: "BackEnd/results/figures/chart_avg_waiting_time.png"
      provides: "Baseline bar chart with 95% CI error bars (D-13)"
  key_links:
    - from: "BackEnd/scripts/run_campaign.py"
      to: "BackEnd/results/campaigns/baseline/results.json"
      via: "save_campaign_results()"
      pattern: "results\\.json"
    - from: "BackEnd/scripts/generate_tables.py"
      to: "BackEnd/results/figures/"
      via: "generate_all_outputs()"
      pattern: "generate_all_outputs"
---

<objective>
Run the baseline reproduction campaign (dry-run then full) against live SUMO, generating
results for FedRL, MARL, SARL, fixed-time, and max-pressure on grid-3x3 and grid-5x5
with 10 MC seeds each. Then generate baseline tables and charts, and checkpoint for user review.

Purpose: EXP-01 requires reproducing SEAL paper baseline results. D-05 mandates a dry-run
gate before committing to full runs. D-06 mandates user checkpoint after baseline before
proceeding to ablations.

Output: Populated baseline campaign directory with results.json, baseline LaTeX tables,
and bar charts. User confirms results are sane before Phase proceeds to ablation runs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-experiment-execution-analysis/08-CONTEXT.md
@.planning/phases/08-experiment-execution-analysis/08-01-SUMMARY.md

<interfaces>
From BackEnd/scripts/run_campaign.py CLI:
```
--campaign baseline          # Run baseline reproduction
--topologies grid-3x3 grid-5x5  # Both topologies per D-01
--n-eval-runs 10             # 10 MC seeds per D-03
--dry-run N                  # Override seeds to N for smoke test
```

From BackEnd/scripts/generate_tables.py CLI:
```
--campaign-dir results/campaigns/baseline
--output-dir results/figures/
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dry-run baseline (1 seed, grid-3x3 only) then full baseline campaign</name>
  <files>
    BackEnd/results/campaigns/baseline/results.json,
    BackEnd/results/campaigns/baseline/config.json
  </files>
  <read_first>
    BackEnd/scripts/run_campaign.py,
    BackEnd/api/evaluation/campaign_config.py,
    .planning/phases/08-experiment-execution-analysis/08-CONTEXT.md
  </read_first>
  <action>
    **Step 1: Dry-run gate (per D-05)**

    Run a 1-seed dry-run on grid-3x3 only to verify the pipeline works end-to-end:
    ```bash
    cd BackEnd && python scripts/run_campaign.py --campaign baseline --dry-run 1 --topologies grid-3x3
    ```

    Check the output for:
    - No Python tracebacks
    - Each config (FedRL, MARL, SARL, fixed-time, max-pressure) produces a result
    - Note the wall-clock time per config from the logs

    If dry-run fails: diagnose the error, fix the issue, and re-run.
    If dry-run succeeds: proceed to full run.

    **Step 2: Full baseline campaign (per D-01, D-02, D-03)**

    Run the full baseline with 10 MC seeds on both topologies:
    ```bash
    cd BackEnd && python scripts/run_campaign.py --campaign baseline --topologies grid-3x3 grid-5x5
    ```

    This will take significant wall-clock time (potentially hours). The script logs progress
    per config. On failure: per D-07, errors are logged in CampaignResult and the script
    continues to the next config. Per D-08, no automatic retries.

    **Step 3: Generate baseline tables and charts (per D-09, D-13)**

    After the campaign completes:
    ```bash
    cd BackEnd && python scripts/generate_tables.py --campaign-dir results/campaigns/baseline --output-dir results/figures/
    ```

    This produces:
    - `results/figures/table_avg_waiting_time.tex` (D-09)
    - `results/figures/table_avg_travel_time.tex`
    - `results/figures/chart_avg_waiting_time.png` (D-13)
    - `results/figures/chart_avg_travel_time.png`
    - `results/figures/chart_mean_reward.png`
  </action>
  <verify>
    <automated>cd C:/Users/user/Documents/GitHub/RoadsidePrediction/BackEnd && python -c "import json; data=json.load(open('results/campaigns/baseline/results.json')); results=data['results']; names=set(r['config']['name'] for r in results); print(f'Configs found: {len(names)}'); assert len(names) >= 5, f'Expected >=5 configs, got {names}'; print('PASS: baseline results.json has all configs')"</automated>
  </verify>
  <acceptance_criteria>
    - BackEnd/results/campaigns/baseline/results.json exists and contains "results" key
    - results.json has entries for at least 5 distinct config names (FedRL, MARL, SARL, fixed-time, max-pressure variants)
    - BackEnd/results/campaigns/baseline/config.json exists
    - BackEnd/results/figures/table_avg_waiting_time.tex exists and contains `\toprule`
    - BackEnd/results/figures/chart_avg_waiting_time.png exists and is > 0 bytes
  </acceptance_criteria>
  <done>
    Baseline campaign results.json populated with all 5 trainer types on both topologies.
    Baseline LaTeX tables and bar charts generated in results/figures/.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User reviews baseline results before ablation runs (per D-06)</name>
  <files>
    BackEnd/results/figures/table_avg_waiting_time.tex,
    BackEnd/results/figures/chart_avg_waiting_time.png,
    BackEnd/results/campaigns/baseline/results.json
  </files>
  <action>
    Display the baseline results for user review. Read and present:
    1. The LaTeX table content from results/figures/table_avg_waiting_time.tex
    2. The chart image from results/figures/chart_avg_waiting_time.png
    3. A summary of config names and error counts from results/campaigns/baseline/results.json
    4. Topology coverage confirmation (grid-3x3 and grid-5x5 both present)
  </action>
  <verify>
    <automated>cd C:/Users/user/Documents/GitHub/RoadsidePrediction/BackEnd && python -c "import json, os; assert os.path.exists('results/campaigns/baseline/results.json'); assert os.path.exists('results/figures/table_avg_waiting_time.tex'); print('PASS: baseline artifacts ready for review')"</automated>
  </verify>
  <acceptance_criteria>
    - User has seen the baseline avg waiting time table and chart
    - User has typed "approved" or provided specific feedback
  </acceptance_criteria>
  <done>
    User has reviewed baseline results and approved proceeding to ablation runs.
  </done>
</task>

</tasks>

<verification>
1. `BackEnd/results/campaigns/baseline/results.json` exists with >= 5 config entries
2. `BackEnd/results/figures/table_avg_waiting_time.tex` contains `\toprule`
3. `BackEnd/results/figures/chart_avg_waiting_time.png` is non-empty
4. User approves baseline results at checkpoint
</verification>

<success_criteria>
Baseline campaign complete with all 5 trainers on both topologies, results persisted as JSON,
LaTeX tables and bar charts generated, and user has reviewed and approved the results before
ablation runs proceed.
</success_criteria>

<output>
After completion, create `.planning/phases/08-experiment-execution-analysis/08-02-SUMMARY.md`
</output>
